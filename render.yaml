services:
  - type: web
    name: streamlit-cold-email-generator
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "streamlit run app/main.py --server.port $PORT --server.address 0.0.0.0"
    envVars:
      - key: GROQ_API_KEY
        sync: false
